spring:
  application:
    name: nook

  datasource:
    url: jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:db_nook}
    username: ${PGUSER:postgres}
    password: ${PGPASSWORD:felipe}
    driver-class-name: org.postgresql.Driver

